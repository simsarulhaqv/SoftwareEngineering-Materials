## Standard Error probes:

- moniter:
	* Divide by zero
	* Array out of bound
	* Acceess on uninitailized variable
	* Parameter argument mismatch.

If the programming environment does not support standard probes, user defined probes are injected as exceptions.
